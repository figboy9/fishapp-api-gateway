version: '3'
services:
  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
    environment:
      TZ: Asia/Tokyo
    ports:
      - "8080:8080"
    tty: true